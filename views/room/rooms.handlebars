<h1>List of Rooms</h1>
<div class="container">
    <div class="row">
        {{#each rooms}}
        <div class="col">
            <a href="/room/room/{{{this.dataValues.idRoom}}}">{{{ this.dataValues.roomNumber }}}</a>
        </div>
        {{/each}}
    </div>
</div>